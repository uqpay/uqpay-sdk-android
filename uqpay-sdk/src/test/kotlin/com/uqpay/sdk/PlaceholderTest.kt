package com.uqpay.sdk

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder so the test source set compiles. Replace with real tests as logic lands —
 * required coverage is listed in docs/acceptance-criteria.md §9.
 */
class PlaceholderTest {
    @Test
    fun placeholder() {
        assertTrue(true)
    }
}
